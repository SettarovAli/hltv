import IconButton from "@material-ui/core/IconButton";

const TouchBackgroundComponent = ({ children }) => {
  return <IconButton>{children}</IconButton>;
};

export default TouchBackgroundComponent;
